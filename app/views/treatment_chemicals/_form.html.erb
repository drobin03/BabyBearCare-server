<%= form_for(@treatment_chemical) do |f| %>
  <% if @treatment_chemical.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@treatment_chemical.errors.count, "error") %> prohibited this treatment_chemical from being saved:</h2>

      <ul>
      <% @treatment_chemical.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
